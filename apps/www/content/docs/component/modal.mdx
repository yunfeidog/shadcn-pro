---
title: Modal
date: 2024-04-25
description: A customizable modal dialog component built with Radix UI Dialog primitive
author: dillionverma
published: true
---

<ComponentPreview name="modal-demo" />

A versatile modal dialog component with multiple variants including success, error, warning, and confirmation dialogs. Built with Radix UI Dialog primitive for full accessibility support and keyboard navigation.

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-pro@latest add modal
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the required dependencies:</Step>

```bash
npm install @radix-ui/react-dialog lucide-react
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="modal" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx showLineNumbers
import { useState } from "react"

import { Modal } from "@/components/ui/modal"
```

```tsx showLineNumbers
function BasicModalExample() {
  const [open, setOpen] = useState(false)

  return (
    <Modal
      open={open}
      onOpenChange={setOpen}
      trigger={
        <button className="bg-primary text-primary-foreground hover:bg-primary/90 rounded-md px-4 py-2">
          Open Modal
        </button>
      }
      title="Example Modal"
      description="This is an example modal dialog with custom content"
    >
      <div className="space-y-4">
        <p className="text-muted-foreground">
          This modal demonstrates the basic usage of the Modal component. You
          can add any content here.
        </p>
        <div className="flex gap-2">
          <button
            onClick={() => setOpen(false)}
            className="bg-secondary text-secondary-foreground hover:bg-secondary/80 rounded-md px-3 py-1"
          >
            Cancel
          </button>
          <button
            onClick={() => setOpen(false)}
            className="bg-primary text-primary-foreground hover:bg-primary/90 rounded-md px-3 py-1"
          >
            Confirm
          </button>
        </div>
      </div>
    </Modal>
  )
}
```

## Features

- **🔧 Accessible**: Built with Radix UI Dialog primitive for full accessibility
- **⌨️ Keyboard Navigation**: Complete keyboard support with focus trapping
- **🎯 Focus Management**: Automatic focus management and restoration
- **📱 Responsive**: Mobile-friendly design with proper viewport handling
- **🎨 Customizable**: Multiple variants and extensive styling options
- **⚡ TypeScript**: Full type safety with comprehensive type definitions
- **✨ Smooth Animations**: Beautiful enter/exit animations and transitions

## Examples

### Basic Modal

<ComponentPreview name="modal-basic" />

### Success Modal

<ComponentPreview name="modal-success" />

### Error Modal

<ComponentPreview name="modal-error" />

### Warning Modal

<ComponentPreview name="modal-warning" />

## Props

### Modal

| Prop           | Type                                     | Default   | Description                       |
| -------------- | ---------------------------------------- | --------- | --------------------------------- |
| `open`         | `boolean`                                | `-`       | Controls the modal open state     |
| `onOpenChange` | `(open: boolean) => void`                | `-`       | Callback when modal state changes |
| `trigger`      | `React.ReactNode`                        | `-`       | Element that triggers the modal   |
| `title`        | `string`                                 | `-`       | Modal title text                  |
| `description`  | `string`                                 | `-`       | Modal description text            |
| `variant`      | `default \| success \| error \| warning` | `default` | Modal variant style               |
| `children`     | `React.ReactNode`                        | `-`       | Modal content                     |
| `className`    | `string`                                 | `-`       | Additional CSS classes            |

## Variants

### Success Modal

```tsx showLineNumbers
<Modal
  variant="success"
  title="Success!"
  description="Your action was completed successfully"
  open={successOpen}
  onOpenChange={setSuccessOpen}
  trigger={<button>Show Success</button>}
>
  <div className="space-y-4">
    <div className="flex items-center gap-2 text-green-600">
      <CheckCircle className="h-5 w-5" />
      <span>Operation completed successfully</span>
    </div>
    <button
      onClick={() => setSuccessOpen(false)}
      className="rounded-md bg-green-600 px-4 py-2 text-white hover:bg-green-700"
    >
      Continue
    </button>
  </div>
</Modal>
```

### Error Modal

```tsx showLineNumbers
<Modal
  variant="error"
  title="Error"
  description="Something went wrong with your request"
  open={errorOpen}
  onOpenChange={setErrorOpen}
  trigger={<button>Show Error</button>}
>
  <div className="space-y-4">
    <div className="flex items-center gap-2 text-red-600">
      <XCircle className="h-5 w-5" />
      <span>Please try again later or contact support</span>
    </div>
    <div className="flex gap-2">
      <button
        onClick={() => setErrorOpen(false)}
        className="bg-secondary text-secondary-foreground rounded-md px-3 py-1"
      >
        Cancel
      </button>
      <button
        onClick={handleRetry}
        className="rounded-md bg-red-600 px-3 py-1 text-white hover:bg-red-700"
      >
        Retry
      </button>
    </div>
  </div>
</Modal>
```

### Warning Modal

```tsx showLineNumbers
<Modal
  variant="warning"
  title="Warning"
  description="This action cannot be undone"
  open={warningOpen}
  onOpenChange={setWarningOpen}
  trigger={<button>Show Warning</button>}
>
  <div className="space-y-4">
    <div className="flex items-center gap-2 text-yellow-600">
      <AlertTriangle className="h-5 w-5" />
      <span>Are you sure you want to continue?</span>
    </div>
    <div className="flex gap-2">
      <button
        onClick={() => setWarningOpen(false)}
        className="bg-secondary text-secondary-foreground rounded-md px-3 py-1"
      >
        Cancel
      </button>
      <button
        onClick={handleConfirm}
        className="rounded-md bg-yellow-600 px-3 py-1 text-white hover:bg-yellow-700"
      >
        Confirm
      </button>
    </div>
  </div>
</Modal>
```

## Advanced Usage

### Confirmation Dialog

```tsx showLineNumbers
import { useState } from "react"
import { Trash2 } from "lucide-react"

import { Modal } from "@/components/ui/modal"

function DeleteConfirmation() {
  const [open, setOpen] = useState(false)
  const [isDeleting, setIsDeleting] = useState(false)

  const handleDelete = async () => {
    setIsDeleting(true)
    try {
      // Perform delete operation
      await deleteItem()
      setOpen(false)
    } catch (error) {
      console.error("Delete failed:", error)
    } finally {
      setIsDeleting(false)
    }
  }

  return (
    <Modal
      variant="error"
      open={open}
      onOpenChange={setOpen}
      trigger={
        <button className="flex items-center gap-2 rounded-md bg-red-600 px-3 py-1 text-white hover:bg-red-700">
          <Trash2 className="h-4 w-4" />
          Delete Item
        </button>
      }
      title="Delete Item"
      description="This action cannot be undone. The item will be permanently deleted."
    >
      <div className="space-y-4">
        <p className="text-muted-foreground text-sm">
          Are you sure you want to delete this item? All associated data will be
          lost.
        </p>
        <div className="flex gap-2">
          <button
            onClick={() => setOpen(false)}
            disabled={isDeleting}
            className="bg-secondary text-secondary-foreground hover:bg-secondary/80 rounded-md px-4 py-2 disabled:opacity-50"
          >
            Cancel
          </button>
          <button
            onClick={handleDelete}
            disabled={isDeleting}
            className="rounded-md bg-red-600 px-4 py-2 text-white hover:bg-red-700 disabled:opacity-50"
          >
            {isDeleting ? "Deleting..." : "Delete"}
          </button>
        </div>
      </div>
    </Modal>
  )
}
```

### Form Modal

```tsx showLineNumbers
import { useState } from "react"

import { Modal } from "@/components/ui/modal"

function FormModal() {
  const [open, setOpen] = useState(false)
  const [formData, setFormData] = useState({ name: "", email: "" })

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    // Handle form submission
    console.log("Form data:", formData)
    setOpen(false)
  }

  return (
    <Modal
      open={open}
      onOpenChange={setOpen}
      trigger={<button>Add User</button>}
      title="Add New User"
      description="Enter the user details below"
    >
      <form onSubmit={handleSubmit} className="space-y-4">
        <div>
          <label htmlFor="name" className="block text-sm font-medium">
            Name
          </label>
          <input
            id="name"
            type="text"
            value={formData.name}
            onChange={(e) => setFormData({ ...formData, name: e.target.value })}
            className="mt-1 w-full rounded-md border px-3 py-2"
            required
          />
        </div>
        <div>
          <label htmlFor="email" className="block text-sm font-medium">
            Email
          </label>
          <input
            id="email"
            type="email"
            value={formData.email}
            onChange={(e) =>
              setFormData({ ...formData, email: e.target.value })
            }
            className="mt-1 w-full rounded-md border px-3 py-2"
            required
          />
        </div>
        <div className="flex gap-2 pt-4">
          <button
            type="button"
            onClick={() => setOpen(false)}
            className="bg-secondary text-secondary-foreground hover:bg-secondary/80 rounded-md px-4 py-2"
          >
            Cancel
          </button>
          <button
            type="submit"
            className="bg-primary text-primary-foreground hover:bg-primary/90 rounded-md px-4 py-2"
          >
            Add User
          </button>
        </div>
      </form>
    </Modal>
  )
}
```
