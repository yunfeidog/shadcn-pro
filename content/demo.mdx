# MDX Demo

import React from 'react'

export const CustomButton = ({ children, onClick }) => (
  <button
    onClick={onClick}
    className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition-colors"
  >
    {children}
  </button>
)

This is **MDX content** that can include React components!

<CustomButton onClick={() => alert('Hello from MDX!')}>
  Click me! 🚀
</CustomButton>

## Interactive Elements

export const Counter = () => {
  const [count, setCount] = React.useState(0)
  return (
    <div className="p-4 border rounded-lg bg-gray-50">
      <p>Count: <strong>{count}</strong></p>
      <div className="mt-2 space-x-2">
        <button
          onClick={() => setCount(c => c + 1)}
          className="bg-green-500 text-white px-3 py-1 rounded"
        >
          +
        </button>
        <button
          onClick={() => setCount(c => c - 1)}
          className="bg-red-500 text-white px-3 py-1 rounded"
        >
          -
        </button>
      </div>
    </div>
  )
}

<Counter />

## Code Examples

Here's how you can use this component:

```tsx
import { Markdown } from '@/registry/yunfei/blocks/markdown'

export default function MyPage() {
  return (
    <Markdown
      content={mdxContent}
      type="mdx"
    />
  )
}
```

## Features

- ✅ **React Components**: Embed interactive components directly
- ✅ **State Management**: Use React hooks like `useState`
- ✅ **Event Handlers**: Add click handlers and other events
- ✅ **Styling**: Full Tailwind CSS support
- ✅ **TypeScript**: Type-safe component definitions

Pretty cool, right? You can mix **markdown** with React components seamlessly!